steps:
  - name: alpine
    args:
      - '-c'
      - |
        echo "*************************************"
        echo "$BRANCH_NAME"
        echo "*************************************"
    id: branch name     
    entrypoint: sh